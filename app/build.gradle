apply plugin: 'com.android.application'

android {
    signingConfigs {
        release {
            storeFile file('E:/ak95/AndroidProjects/androidsign')
            storePassword 'lloret95'
            keyAlias "sign"
            keyPassword "lloret95"
        }
    }

    compileSdkVersion 23
    buildToolsVersion "23.0.3"

    defaultConfig {
        applicationId "com.antonklimakov.metar"
        minSdkVersion 15
        targetSdkVersion 23
        versionCode 6
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    //compile 'com.android.support:appcompat-v7:22.1.1'
    compile 'org.jsoup:jsoup:1.8.3'
}
